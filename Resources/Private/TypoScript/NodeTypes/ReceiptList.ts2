
##
# "ReceiptList" element
#
prototype(FreeVeggie.Blog:ReceiptList) < prototype(TYPO3.Neos:Content) {
    @class = 'FreeVeggie\\Blog\\TypoScript\\ListImplementation'

    filter = 'FreeVeggie.Blog:Receipt'
    selection = ${q(node).property('selection')}
    listRootDocumentQuery = ${q(q(node).property('rootDocument'))}

    items = ${this.items}

    @cache {
        mode = 'uncached'
        context {
            1 = 'documentNode'
            2 = 'node'
        }
    }
}
